# VAPP-48 CI Probe

Temporary non-product diagnostic marker used to trigger the existing pull-request CI so the current Full Workspace Typecheck failure can be captured before applying the minimal React-types correction.

This file must be removed before the VAPP-48 fix is merged.
